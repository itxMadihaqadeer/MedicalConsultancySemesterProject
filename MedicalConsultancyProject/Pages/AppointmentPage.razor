@page "/appointment"
@using MedicalConsultancyProject.Components
@inherits LayoutComponentBase
<Appointment />


<div class="container-xxl py-5">
    <div class="container">
        <!-- Existing HTML Code -->

        <div class="col-lg-6 wow fadeInUp" data-wow-delay="0.5s">
            <div class="bg-light rounded h-100 d-flex align-items-center p-5">
                <form @onSubmit="SubmitAppointmentForm">
                    <!-- Existing HTML Form Code -->
                    <!-- Additional Fields for Capturing Appointment Data -->
                    <input type="hidden" bind="@Appointment.DoctorId" />
                    <input type="hidden" bind="@Appointment.SelectedDate" />
                    <input type="hidden" bind="@Appointment.SelectedTime" />

                </form>
            </div>
        </div>
    </div>
</div>

@code {
    private Appointment Appointment = new Appointment();

    private void SubmitAppointmentForm()
    {
        // Handle the form submission, you can save the appointment data, send emails, etc.
        // Access the appointment data through the "Appointment" property.
        // For example, you can send it to a backend API, save it to a database, etc.
    }
}